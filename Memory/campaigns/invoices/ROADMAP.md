# Invoices campaign roadmap

Jafar authorized Jobber as the behavior baseline and approved the campaign direction on 2026-09-05. Reuse shipped
Quote/Job patterns; add only Invoice-specific truth. Part 2's corrected design is approved and Part 3
implementation is under way, split into 3a/3b/3c on Jafar's 2026-09-04 approval.

| Part | Outcome                                                                   | State                                          | Dependency                                     | Completion gate                                                                                 |
| ---- | ------------------------------------------------------------------------- | ---------------------------------------------- | ---------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| 1    | Verify Jobber and approve the Invoice behavior contract                   | Complete — redline and concrete D1–D5 approved | Live Jobber, official docs, existing contracts | Status, source, snapshot, money, delivery and scope boundaries are explicit                     |
| 2    | Design Invoice foundation and billing-address seam                        | Complete — corrected design redline approved | Part 1; corrected design approval           | Smallest tenant-safe model, commands, permissions and test matrix approved                      |
| 3a   | Build Invoice identity, terms, numbering, snapshots, arithmetic, draft and issued-document commands, plus private command receipts and retry protection | Complete 2026-09-05 — three migrations applied to the remote database, 72/72 pgTAP | Part 2                    | Met: isolation, numbering, calculation, snapshot freezing, issued edits retaining prior snapshots and replay safety are verified. Void/Bad debt/Mark received moved to 3b with the ledger their rules depend on; their seams are wired here |
| 3b-1 | Build the money ledger, the three balances, and the commands that record, apply, unapply and move money | Complete 2026-09-05 — two migrations applied to the remote database, 80/80 pgTAP, balance plans measured | Part 3a | Met: receipts, allocation, unapplication, movement, the three balances, D1 draft rules, deposit reuse, append-only privilege, isolation and replay verified |
| 3b-2 | Build refunds, receipt reversal, and the payment-dependent lifecycle commands (Void, Bad debt, Mark received) | Complete 2026-09-05 — one migration applied to the remote database, 89/89 pgTAP, plans measured | Part 3b-1 | Met: refund caps against the original receipt, reversal of a receipt or an erroneous refund, D2's Void refusal with its deposit release, bad debt/unmark, mark received/reopen, permissions, isolation and replay verified |
| 3c   | Build source claims and correction/rebill replacement chains              | Complete 2026-09-05 — one migration applied to the remote database, 60/60 pgTAP, index plans measured; not yet committed to Git | Parts 3a–3b; Jobs 11c for installment references only | Met: one work unit billed once, claims retained after Void, rebill succeeds once (no chain branching), progress-invoice exclusion from ordinary Void enforced; uniqueness probe, chain-claims and successor lookups each verified index-only/index scan at ~150k rows |
| 4a   | Deliver the Invoice list (read model + list screen)                        | Complete + committed 2026-09-05 (d947a3a) — migration applied, plans measured at 40k, 3c pgTAP re-verified 60/60 | Parts 3a–3c | Met: keyset list, live status overview, search/filter/sort, no money leak; list & overview derive status one way |
| 4b   | Deliver the new-invoice form and the detail page                           | Complete + committed 2026-09-05 (a7bdb1d) — type-clean, all flows browser-verified; delete-hang bug fixed | Part 4a | Met: new form + detail screen, draft-only edits (lines/discount/tax/subject/terms), Mark as Sent, Delete draft; read model gates money; no duplicate UI |
| 5    | Deliver Job, Visit, reminder and installment handoff                      | Split 5a/5b/5c on Jafar's approval 2026-09-05, who also approved building Jobs 11c and 13b inside this campaign rather than duplicating them there | Parts 3a–3c, 4; Jobs 11c (now owned by 5c)  | Eligible work copies once, reminders resolve, deposits allocate, retries do not duplicate       |
| 5a   | Bill a whole job: billable-work picker, seeded invoice form, create+claim in one transaction, reminders resolved as `invoiced` | Complete + committed 2026-09-05 (fba33ca) — migration applied to remote DB; `npm run check` 0 errors, Prettier clean; browser-verified end to end on Raad LTD Job #1 (menu → dialog → seeded composer → invoice #6 created, job left Requires invoicing → Unscheduled, reminder resolved). Seed-adoption bug fixed in the shared line block + ClientPicker (no-ops for other create forms). | Parts 3a–3c, 4 | Met: `job_total` claims only; one job billed once; retry returns the first invoice; the job leaves Requires invoicing |
| 5b   | Per-visit and billing-period invoicing, Invoice now/later at visit completion, per-line service dates, the ready-to-bill queue | Split 5b-1…5b-5 on Jafar's approval 2026-09-05 (the money engine — `visit`/`reminder_period` claims + period→visit consumption — already shipped/pgTAP-tested in 3c; 5b is read-model + UI + line-seeding, not new money logic) | 5a; Jobs 13a | Completed visits bill once, a period consumes its visits, service dates travel to the bill |
| 5b-1 | Per-line service dates: opt-in `showServiceDate` field on the shared `ProductsAndServicesBlock`, threaded through InvoiceForm (new + seed) and the detail draft editor; renders on the customer document ("Serviced <date>") | Code complete 2026-09-05 — svelte-check 0 errors, Prettier clean, ESLint clean bar the file's pre-existing `Set` lint. Block spec unrun (no Playwright browser here). NOT browser-verified, NOT committed. DB (`create_invoice_draft`/`replace_invoice_lines`) + customer doc already stored/rendered it; this part is the editor UI + carry-through only. | 5a | Set a per-line service date in the new-invoice form and on a draft's detail editor, save it, and see it on the customer invoice; quotes/requests/jobs show no such field |
| 5b-2 | Visits card on the job + per-visit billing: picker surfaces individual completed unbilled visits; seeded invoice copies the job's lines stamped with each visit's date; `visit` claims | CLOSED 2026-09-05, browser-verified, committed `d80d76b`; its over-resolving reminder bug was backfixed with 5b-3 in `37d3992`. | 5b-1; Jobs 13a | Met: completed visits bill once; service dates travel to the bill |
| 5b-3 | Billing-period invoicing: picker offers "bill this period"; `reminder_period` claim sweeps up the period's completed visits (engine already built in 3c) | CLOSED 2026-09-06, browser-verified, committed `37d3992` together with the migration that makes a partial bill resolve only the reminders it answered (17 pgTAP assertions). | 5b-1, 5b-2 | A period consumes its visits; no visit inside it bills separately |
| 5b-4 | Invoice now / later at visit completion (prompt at completion → bill now or defer via reminder) | **CLOSED 2026-09-06**, browser-verified, committed `9fdc7ca`. Frontend only — no migration: `complete_job_visit` already raises the `per_visit` reminder (the "later" outcome), and the seeded composer + 5b-3 scoping already claim the visit and clear that reminder (the "now" outcome). New `VisitInvoicePromptDialog.svelte`; `JobVisitsSection` opens it after a non-replay completion when `canInvoiceVisits && price_basis='per_visit' && billing_timing='per_completed_visit' && client`. Never collides with the one-off FinalVisitDialog (different job types). Browser-verified on job #10 (Tester Account, per-visit / after-each-completed-visit, two past visits): prompt shows amount + client + visit date; "Invoice now" seeds `/invoices/new?client&job&visits=<id>` with the line + that visit's service date and, on save, clears only that visit's reminder; "Invoice later" and backdrop-close both leave the reminder and keep the job in Requires invoicing; light + dark both clean. | 5b-2 | Completing a visit can start its bill or leave a reminder |
| 5b-5 | Ready-to-bill queue: one cross-client list of work requiring invoicing (scale-sensitive → performance-review design branch) | **CLOSED 2026-09-06**, browser-verified. Migration `20260907120000` applied to remote DB: `private.job_uninvoiced_work` (states the three amount rules once — whole job / per completed unbilled visit / per due pending period), `public.ready_to_bill_page` (keyset, oldest-due first, search only), `public.ready_to_bill_count`, and one partial index `ready_to_bill_queue_idx (organization_id, due_on, id) where status='pending'`. Screen at `/(app)/invoices/ready-to-bill`, reached from a "Ready to bill (N)" button on the Invoices header; row per job with client, job, what's waiting, how long, and the uninvoiced amount; "Bill" goes to the existing flow (whole job → `/invoices/new?client&job`, visits/periods → the job page's own card). `InvoiceForm` now invalidates the `['invoices','ready-to-bill']` prefix after a seeded save. Scope Jafar deferred: extra sorts, a client filter, batch checkboxes. | 5b-2 | Met: verified on Raad LTD both row shapes (job #10 per-visit showing 1 of 2 visits at $50 — the partially-billed case — and job #11 whole job at $275); the queue's amount matched the billing picker's; billing job #11 removed it from the queue and dropped the badge to 1; search by client/title/job-number and its empty state; light + dark; no console errors. Measured: see the performance note below. |
| 5c   | Jobs 11c (payment schedule installments, per-visit amounts) and progress invoicing from an installment | Planned | 5a; Jobs 11a | Installments total the job, issued installments lock, each bills once |
| 6    | Deliver email, mark-sent, secure view, PDF and receipt flow               | In progress — 6a done; 6b split into 6b-1 (send/link/view) + 6b-2 (receipts); 6b-1 active | Part 4; Communications email                   | Issue/delivery/view facts and frozen customer document are verified                             |
| 6b-1a| Send/link/view plumbing: enqueue_invoice_communication_email, issue_invoice_access_link, record_invoice_link_view; email + access-links + public view routes; client api; public view-ping; InvoiceEmailDialog; invoice_detail widened with `delivery` + client email | Complete 2026-09-06 — migration applied to remote DB, `npm run check` 0 errors, Prettier clean, svelte autofixer clean; NOT yet committed to Git | 6a | Met: migration applied, all functions/routes/api/component type-clean. UI not yet wired (6b-1b) |
| 6b-1b| Wire the invoice detail page: Send (issue-on-send for a draft, then queue) as the draft primary action; Copy client link + Resend on issued bills; show Sent / Viewed facts. Then browser-verify. | **Complete 2026-09-06**, committed. Page wiring browser-verified earlier (menu items, Copy link, Viewed fact, Resend dialog). The enqueue bug fix (`owner_user_id` → org-default sender) is now applied to remote DB; confirmed live by actually sending invoice #1 (Raad LTD) — clean `201 queued`, delivery intent created with the correct org-default sender, no crash. Residual: the "no sender ready" 422 refusal path (`email-send-errors.ts`) is still only type-checked, not exercised live — every org with an issued invoice in this DB now has a working sender, so there's nothing to test it against without fabricating data. | 6b-1a | Met. Residual noted above is not blocking — same shape as the already-shipped quote 55000 mapping. |
| 6c   | Invoice contract/disclaimer: enter, save, edit, display on the customer invoice, mirroring the shipped quote disclaimer. Separate from payment terms; content frozen when the invoice is issued. | Complete + committed 2026-09-06 (6dee4f4) | Part 4b (detail page), 6a (customer document); Quote disclaimer pattern | Met: draft SectionBlock enter/edit (bottom-bar save, same revision chain as terms); `update_invoice_contract_disclaimer` refuses once `document_frozen_at` is set; customer document + staff preview show it as its own footer, separate from the payment-terms line |
| 6b-2a | Premium hosted receipt page (`/r/[token]`) + receipt email (link, not PDF) + "Save and email receipt" on CollectPaymentDialog. Field set follows Jobber exactly (business header, Transaction-date badge, RECIPIENT name+billing address, "Payment receipt", Paid amount, date/method/reference, details note — no receipt#, no invoice#, no line items, no balance). Visual = invoice customer-document paper shell. | **Complete 2026-09-06**, committed `e1cf299`. Migration `20260906160000` applied to remote DB (registered there as `20260906001300`). Browser-verified: payment records + balance updates; receipt email queues with correct subject/recipient/link; `/r/<token>` renders premium in light + dark, no console errors; malformed/unknown token → one identical "not available" page. Actual email **send** is blocked by a pre-existing org gap — Raad LTD's package resolves `essential_limit_state: not_included`, so the worker defers with backoff (correct behavior); this is communications-activation's problem, not 6b-2a's. | 6b-1; Part 7 payments | Met: receipt document, hosted page, email enqueue, and "Save and email receipt" all verified end to end short of the org email entitlement. |
| 6b-2b | Read-only payment detail page `/(app)/payments/[id]` (amount, method, date, reference, details, Applied to: Invoice #N) with **Send receipt** + **Print/Save PDF**; wire invoice financial-history rows to link there. Payment edit/delete deferred to a later ledger-correction part. | **Complete 2026-09-06** — migration `20260906170000` applied to remote DB (two rows there: `payment_detail_read_model`, `invoice_detail_payment_history_links_payment`). New `public.payment_detail` (refuses without `invoices.view_price`; refunds/reversals are P0404) and `public.payment_receipt_preview` (staff print, same builder as the customer page); `invoice_detail` history rows gained `payment_event_id`. `GET /api/payments/[id]`, `fetchPayment`, `/(app)/payments/[id]`, `/(app)/payments/[id]/receipt` (shell-breakout `+page@.svelte`). `npm run check` 0 errors, Prettier + autofixer clean, 1723 unit tests pass. Browser-verified end to end: history row → payment screen → Applied-to link back, View receipt / Print, Send receipt (201 + "Receipt emailed" toast + delivery intent sent through Brevo). | 6b-2a | Met |
| 6a   | Customer invoice document + secure token view + Preview-as-client + Print/Save PDF | Complete + committed 2026-09-05 (3959681) — browser-verified (light + dark, no console errors, money math correct, print CSS clean) | Part 4; Quote customer-document/token/preview pattern | Met: frozen customer document renders premium; staff preview renders via `invoice_customer_preview`; print CSS strips chrome. Public `/i/[token]` resolver built; live token test defers to 6b (issuer). Money-withheld path code-verified (owner has view_price) |
| 7a   | Collect Payment (single invoice) + financial history read model/display; backend commands already built/tested in 3b-1/3b-2 | Complete + committed 2026-09-06 (d5a0250) — browser-verified end to end on invoice #1 (partial then full payment, balance + status + toast + method labels + history list all correct); date-display off-by-one fix folded in (see below); 6c delivery/email regression fix verified live | Parts 3a–3c, 4–6 | Met |
| 7b   | Void (with reason + D2 refusal), Bad debt/restore, Mark received/reopen   | Complete + committed 2026-09-06 (d324946) — browser-verified end to end on Raad LTD (all five transitions, both undos, D2 refusal in dialog, dark mode). One `/api/invoices/[id]/lifecycle` route (discriminated `action`), one `InvoiceLifecycleDialog` on `ConfirmDialog`, status closure banner on the detail screen; `can_void`+`can_bad_debt` on the GET route; migration `20260906150000` adds `write_off_note` to `invoice_detail`. `npm run check`/eslint/prettier/autofixer clean. | 7a | Met: every Jobber close/reopen transition reconciles through the pgTAP-tested 3b-2 commands; void reason enum `duplicate`/`created_in_error`/`client_request`/`other`; D2 refusal ("still has payments on it") surfaced in the dialog. Deferred follow-up: the void→client cancellation email (needs a new template). |
| 7    | Deliver manual collection, overdue, bad debt, void and reopening          | 7a complete + committed (d5a0250); 7b complete + browser-verified, commit pending. Spreading one payment across a client's several open invoices (Jobber's real behavior) is explicitly deferred — build as a follow-up once single-invoice collection is used. Void→client cancellation email also deferred. | Parts 3a–3c, 4–6 | Partial/full payments, balances, reversals and every Jobber state transition reconcile          |
| 8    | Deliver batch create and batch deliver                                    | Split 8a/8b on Jafar's approval 2026-09-06     | Parts 5–7                                      | Reviewed drafts group compatible Client work; completion atomic with creation; sending separate |
| 8a   | Batch create: selection on the ready-to-bill queue → many drafts at once  | **Closed 2026-09-06** (committed 9f0ae89)      | 5b-5 (the queue), 5a (create_invoice_from_work) | One invoice per client per tax group; explicitly-ticked unfinished visits complete atomically with creation; a group over 100 sources refuses by name; 25-job cap; heavy batch measured |
| 8b   | Batch deliver: select draft/awaiting/past-due invoices → issue + queue emails | **CLOSED 2026-09-06** (committed a4d057b). Backend: migration `20260908110000` (`invoice_batch_deliverable_page`), `GET /api/invoices/deliverable`, `POST /api/invoices/batch/deliver` (shared 20/5min bucket, issue-if-draft, enqueue, stop-at-first-denial). Frontend: `/(app)/invoices/send` picker (nothing selected by default, missing-email flag+block, previously-sent marker, cap 20) + result view retrying only not-queued items under the same keys; "Send invoices" button on the list + warm route. Verified by `deliver.spec.ts` (6 tests): real batch logic over isolated made-up invoices, limiter seeded near threshold, outbox enqueue stubbed (no real email, live bucket untouched) — proves queue-once, denial-skips-rest-untouched, same-key-retry-no-duplicate, one-failure-doesn't-block. `npm run check` 0 errors, prettier clean, full suite 1789 passing. | 8a; 6b-1 (issue + enqueue) | Met. DEFERRED (known Jobber diff): billing-contact fan-out (sends to primary email only) — `Memory/deferred/invoice-email-sends-to-primary-only-not-billing-contact.md`. |
| 9    | Verify integrated billing journeys and measured performance               | Planned                                        | Parts 2–8                                      | Direct, Job, recurring, progress, delivery, payment, exception and batch journeys pass          |

Approved behavior: `docs/invoice-behavior-contract.md`, including D1–D5. Part 2 corrected design:
`docs/invoice-part-2-design.md`. Part 2 is complete, including all seven corrections. Jafar approved the
3a/3b/3c split on 2026-09-04 with these boundaries: private command receipts and retry protection are built in
3a and applied to money commands in 3b; permitted issued-document edits with retained prior snapshots belong to
3a; payment-dependent lifecycle rules are verified in 3b, never declared complete in 3a; source claims and
replacement chains stay in 3c with Jobs 11c dependencies explicit. The split changes build order only, not
approved behavior. Batch groups compatible tax rates, and incomplete Visit completion follows the approved
atomic, previewed permission boundary.

Part 6 decisions (approved 2026-09-05): PDF = browser print/save (matches Jobber's "Print or Save PDF"
affordance and the shipped Quote pattern); no server PDF engine. Build a REUSABLE customer-document visual
foundation (paper shell, brand header, party/address blocks, line-table styling, totals block, print CSS) so
Quote can later adopt it — quote/invoice stay as two purpose-built components, never one mode-flagged
component. Customer sees their own invoice amounts (no view_price gating on the customer's own bill).

Deferred outside this campaign: SMS until Communications activation; online processors, saved methods, automatic
charging, settlements, disputes and payouts until their provider topology receives separate approval.
Retrofit `CustomerQuoteDocument` onto 6a's shared document foundation — small follow-up AFTER Jafar approves
the invoice look; kept out of this campaign because it touches shipped, tested quote code.

Build order (confirmed by Jafar 2026-09-05): Void, Bad debt and Mark received are built with the ledger their
rules read, not in 3a. Jafar also approved splitting 3b into 3b-1 (ledger, balances, money movement) and 3b-2
(refunds, reversal, payment-dependent lifecycle), and approved storing the effective-receivable predicate once
as a calculated column on `invoices` rather than restating it in each caller. Approved behavior is unchanged.

Three facts the ledger parts established that later parts must not contradict:

- Unapplying the money that settled a never-issued draft is refused, because 3a made recognition
  irreversible. 3b-2's refund path must reach that money through a refund of the receipt, not an unapply.
- Money can still be unapplied or moved off a *replaced* invoice, per D3; only a voided one is closed to
  both directions.
- 3b-2 refuses Void on a draft, a replaced bill, a written-off or by-hand-closed bill, and a fully paid one,
  and it cannot yet exclude progress invoices because no installment link exists until Jobs 11c. 3c owns that
  exclusion. Mark received deliberately leaves `is_effective_receivable` true and is gated on
  `invoices.record_payment`.

5b-5 performance evidence (measured 2026-09-06 against the dev project, in a rolled-back transaction seeded
with 40,003 jobs, 60,030 visits and a 90,002-reminder due backlog — a backlog no contractor who invoices at
all would carry):

- The first shape written — gather every due reminder, de-duplicate to one row per job, join the amounts,
  then sort and take 25 — cost **4,171 ms / 424,119 buffers**, because the per-job amount is a lateral and
  Postgres computed it for all 40,001 candidates before the LIMIT. An index scan was present throughout; the
  plan shape, not the access method, was the problem.
- Shipped shape (find the page, then price it): first page **50 ms / 893 buffers**, page 200 via cursor
  **4 ms / 454 buffers**, count badge **68 ms / 41,623 buffers** (index-only, no join).
- Known ceiling, accepted and not optimized: a **search** term cannot be served by the index and walks the
  backlog probing each candidate's job — **326 ms / 184,933 buffers** at 90k due reminders (163 ms when the
  planner flips to jobs-first for a term matching nothing). Bounded by the backlog, and a deliberate user
  action rather than what the screen does on load. Revisit only if a real tenant's backlog approaches this.

Two reminder gaps found while scoping 5b-5, flagged and NOT fixed here (they belong to Jobs, not Invoices):
billing set to "On dates we pick ourselves" raises no reminder until somebody adds a date, and "Once, when
the job is finished" raises none until the job is actually closed — in both cases finished work can sit with
nothing flagging it. "Never remind us" is a deliberate opt-out and is correctly absent from the queue.

Part 8 decisions (Jafar, 2026-09-06, after a live read-only tour of Jobber's two batch screens — findings
written permanently into `.claude/skills/jobber/jobber-05-invoices-payments.md` §"Batch invoicing"):

- **Grouping = Jobber's.** One invoice per client; a client's several jobs merge onto one bill; differing
  property tax rates split it. Each line keeps its own job's service date.
- **Unfinished visits follow the already-approved contract:** explicitly selectable, invoiced and marked
  complete in the same save. Selecting a client/job must NEVER auto-select them; they are labelled, and a
  short notice appears before saving ("…will also mark 3 selected visits complete"). Creation and completion
  succeed or fail together, and that must be tested. No extra configuration or approval workflow.
- **Cap 25 selected jobs per batch.** "Select all" = current page only, with the count shown. Existing
  loading state, no duplicate submission. No background queue, no new progress system.
- **The per-invoice 100-source claim limit is a separate limit and does not follow from the 25-job cap** —
  25 jobs can hold more than 100 billable items. Validate it explicitly and verify a representative heavy
  batch before claiming any timing.
- **Out of scope, permanently:** Jobber's "Standard Mail" (combined printable PDF + Avery address labels).
  We do not do postal mail and the approved no-server-PDF-engine decision rules out the combined file.
