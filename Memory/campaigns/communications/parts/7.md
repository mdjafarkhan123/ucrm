# Part 7 — closed at code level (2026-08-28)

Every slice 7.1–7.6b passed its acceptance at code level; both screens sit on ROADMAP.md's pending
browser list for the Part 9 pass. Slice detail lives in git, the migrations, and the pgTAP files.

Kept here only because a fresh session cannot rediscover it:

- The live claim function is migration 20260824100947's plpgsql version, re-`create or replace`d by
  7.1, 7.3, 7.4 and 7.5b. **The current body is in `20260906230000_communications_email_provider_period_capacity.sql`** —
  edit that one.
- Effective-dated config tables (reputation thresholds, warm-up stages, platform sending settings)
  reject two `set_*` calls in ONE transaction: same `now()` fails `effective_to > effective_from`.
  Never happens through the API; pgTAP must not supersede a row it created in the same txn.
- The 7.1–7.6 tables carry no `authenticated` grant. Contractor-facing reads go through the owner
  (service-role) client with an explicit `organization_id` filter, not RLS.
- `communication_inbound_messages` has a `..._resolution_complete` check: a pgTAP inbound fixture
  needs its resolution columns (sender_id / reply_alias_id / owner_user_id), not just client +
  contact method.
- Warm-up clock = `coalesce(warmup_started_at, verified_at)` on the sending domain, counted by UTC
  calendar day. `warmup_started_at` is reserved for a future lifecycle step-back and is unused.
- The HTTP email-worker transport is still a stub (`ready:false`). Part 7's DB layer runs ahead of it,
  so settings/owner UI — not live mail — is the verifiable surface.
- Local and remote migration names diverge (local `2026090XXXXXXX`, remote a real timestamp).
